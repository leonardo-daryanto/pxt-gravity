namespace sprites {
     /**
      *  make the right gravity
      */
    //% block
    //% group="Advanced"
    export function makeNormalGravity(): number {
        
        
        return 400
    }
    /**
     * use how many amount of gravity 
     * e.g. 400.5
     */
    //% block
    export function makeGravityTo(gravity: number): number {
        return gravity
    }
}
